spring:

  application:
    version: 0.0.1
    name: hd-platform

  profiles:
    active: local

server:

  port: 8080

management:

  endpoints:
    web:
      exposure:
        include: health,info

springdoc:

  swagger-ui:
    path: /swagger-ui.html

logging:

  level:
    root: INFO